{% extends "layout.twig" %}

{% block content %}
  <div class="card">
    <p class="muted">{{ message }}</p>
    <p><a href="{{ base_path }}/accounts">Back to accounts</a></p>
  </div>
{% endblock %}
